//radar functionality//
void setup() {
  //setup radars pinout and input reading

}

void loop() {
  //radar 1 will be pointing at user(side of motors 3 and 4)
  /*
  while(radarvalue > [specified distance]){
    // ESC3.speed(2000);
    // ESC4.speed(2000);
  }
  //ESC3.speed(1500);
  //ESC4.speed(1500);

  while(radarvalue < [Specified distance]){
    //ESC1.speed(2000);
    //ESC2.speed(2000);
  }
  //ESC3.speed(1500);
  //ESC4.speed(1500);
  */
  //radar 2 will be pointing awy from user, at objects(side of motors 1 and 2)


}
